pragma solidity ^0.4.2;
contract contentProtocol {
    //only owner of content (linked content contract to address mapping) can mint token with content identifer
    //is minted on view
    //dileniate based on vT protocol ~ share percent etc
}