pragma solidity ^0.4.0;
contract personalIdentiferProtocol {
    //only owner of wallet address can mint token with wallet identifer
    //can only mint 86400 tokens per day
    //based on datetime object for helper contract
}